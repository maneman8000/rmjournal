[project]
name = "rmjournal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "rmscene>=0.7.0",
]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
